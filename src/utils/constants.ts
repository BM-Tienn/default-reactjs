export enum TYPE_LOCAL_STORAGE {
  URL_REDIRECT = 'default-url-redirect',
}

export enum TYPE_COOKIE {
  TOKEN = 'default-token',
  DEVICE_ID = 'default-device-id',
  AFFILIATE_ID = 'default-affiliate-id',
}
