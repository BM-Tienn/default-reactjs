/**
 *
 * styled UploadDropzone
 *
 */
import styled from 'styled-components/macro';

export const UploadDropzoneWrapper = styled.div`
  .dropbox {
    color: var(--color-smoke);
    border: 2px dashed rgb(255 255 255 / 0.2);

    &:hover {
      color: #fff;
      border-color: rgb(255 255 255 / 0.5);
    }
  }
`;
