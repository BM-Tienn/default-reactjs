/**
 *
 * styled Search
 *
 */
import styled from 'styled-components/macro';

export const SearchWrapper = styled.div<{
  width: number | 'auto';
  borderRadius: number;
  size: 'sm' | 'md' | 'lg';
  theme: 'light' | 'dark';
}>`
  }
`;
